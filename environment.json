{
  "language": "python",
  "python_version": "3.12",
  "dependency_manager": "pip",
  "install_command": "pip install -e .[dev]",
  "test_command": "pytest -q",
  "lint_command": null,
  "notes": "Deterministic Python library with openpyxl>=3.1 as a runtime dependency; the [dev] extra adds pytest==8.3.4 for testing. Seeds the future environment manifest (ADR-024)."
}
