{
  "language": "python",
  "python_version": "3.12",
  "dependency_manager": "pip",
  "install_command": "pip install -e .[dev]",
  "test_command": "pytest -q",
  "lint_command": null,
  "notes": "Deterministic library with openpyxl as a runtime dependency (pytest is the only dev dependency). Seeds the future environment manifest (ADR-024)."
}
