{
  "language": "python",
  "python_version": "3.12",
  "dependency_manager": "pip",
  "install_command": "pip install -e .[dev]",
  "test_command": "pytest -q",
  "lint_command": null,
  "notes": "Installable HTTP service. Start Redis and Postgres separately (without docker-compose), then configure REDIS_URL (default redis://localhost:6379) and DATABASE_URL (default postgresql://localhost:5432/postgres). Launch with python -m trek_fixture.server."
}
