{
  "language": "python",
  "python_version": "3.12",
  "dependency_manager": "pip",
  "install_command": "pip install -e .[dev]",
  "test_command": "pytest -q",
  "lint_command": null,
  "notes": "Deterministic library with Pint as a runtime dependency; the editable development install also includes pytest. Seeds the future environment manifest (ADR-024)."
}
